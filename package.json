{
  "name": "nicinabox",
  "version": "2.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\"",
    "start": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy",
    "clean": "mkdir -p build && rm -rf build/*",
    "publish": "npm run update && npm run build && npm run deploy",
    "deploy": "node tasks deploy",
    "update": "node tasks data"
  },
  "dependencies": {},
  "devDependencies": {
    "@11ty/eleventy": "^0.11.0",
    "@auth0/s3": "^1.0.0",
    "@babel/core": "^7.10.3",
    "@babel/preset-env": "^7.10.3",
    "@babel/register": "^7.10.3",
    "bootstrap-sass": "^3.4.1",
    "date-fns": "^2.14.0",
    "github-api": "^3.3.0",
    "glob": "^7.1.6",
    "handlebars": "^4.7.6",
    "node-sass": "^4.14.1"
  },
  "author": "nicinabox",
  "license": "MIT"
}
